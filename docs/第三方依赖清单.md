# 第三方依赖清单

运行时主要依赖：Streamlit、Pandas、Plotly、SQLAlchemy、Alembic、Pydantic、Requests、BeautifulSoup、Trafilatura、pypdf、Jieba、NumPy、scikit-learn、NetworkX、Markdown。开发依赖为 Pytest、Ruff、Mypy、pip-audit 与 Playwright。

精确解析版本以 `uv.lock` 为准；第三方数据授权边界见仓库根目录 `THIRD_PARTY_NOTICES.md`。
