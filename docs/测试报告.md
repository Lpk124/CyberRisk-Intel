# 测试报告

本文件只记录 2026-09-01 在 Windows、Python 3.12.13 环境中的真实执行结果。

## 自动化验证

- `uv build`：通过；生成 sdist 和 wheel。
- `ruff check .`：通过，0 项。
- `mypy src`：通过，35 个源文件无类型错误。
- `pytest --cov=cyberrisk_intel`：13/13 通过，总覆盖率 69%。
- `pip-audit`：已审计锁定环境，无已知漏洞；本地未发布包按预期跳过。
- 凭据形态扫描：未发现 API key、AWS access key 或私钥形态内容。

## 运行与浏览器验证

- CLI 初始化与连续两次 `seed-demo` 成功；两次结果均为 10 条政策、12 个事件、4 个漏洞、3 个 KEV、213 条关系和 58 个检索分块。
- JSON 导入 CLI 实测：政策 10 条、事件 12 条均幂等更新，失败 0；重建索引后计数不变。
- Alembic 初始迁移已在独立空 SQLite 数据库升级到 `e67c4b25c7bc (head)`。
- 官方在线同步实测：CISA KEV 发现 1,687 条，失败 0；MITRE Enterprise ATT&CK
  发现 873 个战术/技术对象，失败 0。同步后本地库含 1,688 个漏洞、858 个 ATT&CK
  技术与 2,593 个统一检索分块。
- 再次同步生成 2 个内容寻址原始快照及对应 `raw_document`，重复内容不新增审计记录；
  provenance 回归测试通过。
- Python Playwright 使用本机 Edge，在真实 Streamlit 服务器上通过总览、检索、安全事件、关系探索、RAG 助手和报告页烟雾测试。
- 人工查看总览与报告截图，中文、指标、时间图、导航和报告排版正常。

## 尚未宣称完成的指标

演示数据尚未达到 30 条政策、100 个事件和 100 个事件相关非 KEV CVE；RAG 50 题黄金集及 Recall@5 ≥ 0.85 因此尚未执行。不得将其写作已完成成绩。
